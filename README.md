# DataFabric
A simple, extensible app that lets users define their own entity schemas, add records against those entities, and produce ad-hoc reports. Designed for easy extension so domain-specific logic can be plugged into entity lifecycle events.
